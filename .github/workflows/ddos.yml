name: "Simple Test"
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Start"
      - run: python3 -c "print('Python works')"
      - run: python3 -c "import socket; print('Testing socket'); s=socket.socket(); s.settimeout(2); s.connect(('1.1.1.1',80)); print('Connected to 1.1.1.1'); s.close()"
